# Repository Inventory

This inventory records the files currently present in `unreal-kz/kbtu-guest-lecture-project` and how they are being used for guest lecture preparation.

## Current files

### 1. `Syllabus-scan.jpeg`
- Repository role: raw scan image.
- Use in planning: primary visual source stored in the repo.
- Reliability note: image content should be treated as source evidence; where text is hard to read, the reconstructed script/PDF helps clarify the structure.

### 2. `generate_syllabus_page_4_pdf.py`
- Repository role: script that reconstructs page 4 into a clean PDF.
- Use in planning: main machine-readable source for extracting:
  - course title;
  - weekly topics;
  - assessment schedule;
  - the explicit entry `ML-enabled IS & Evaluation Metrics`.

### 3. `syllabus-page-4.pdf`
- Repository role: generated artifact of the reconstructed page.
- Use in planning: supporting human-readable reference file that corresponds to the reconstruction script.

## Repo-grounded planning consequence

The strongest directly supported guest lecture anchor in the current repo is:

- **Week 11: ML-enabled IS & Evaluation Metrics**

This is preferable to broader topic assumptions because it is explicitly present in the repository content.
